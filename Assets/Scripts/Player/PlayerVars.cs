using UnityEngine;

// I will see about using this script to hold various objects that the player needs like input actions.
public class PlayerVars : MonoBehaviour
{
    //private ProjectActions projectActions;
    
    private void Awake()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
